# Termi-Watch
TermiWatch is a real-time security monitor that intercepts "ghost" terminal windows. It uses WMI event-driven hooks to catch CMD or PowerShell instances the millisecond they open, allowing you to inspect their command-line arguments and kill them before they can execute malicious scripts.
